## 目标
- 将第一幕·室内房间（场景一醒来 + 场景二日常）的真实剧本，按对话系统 CSV 规范结构化并导入 Unity，产出可运行的 `DialogueGraphData` 资源。
- 同步固化项目制作流程、命名规范与路线图文档。

## 变更点
- 新增 `Plans/开发路线图与制作规范.md`：项目总指挥文档（进展快照、制作工作流、命名规范、P0–P2 路线图、Editor 待办）。
- 新增 `Assets/Resources/dialogue/Act1/Act1_Room_Nodes.csv`（26 节点）与 `Act1_Room_Choices.csv`（6 选项 / 3 组），内容取自《潜抑》第一幕剧本，按"幕场景"五位编号（1xxxx）。
  - 入口段：开场 11000、镜子 11100、冰箱 11200、书桌小说 11300、抽屉 11340、香烟 11400、手机 11500。
  - 应激度按剧本系统提示落表：吃饺子 -3、阅读 -1、抽烟 -4，其余 0。
- 经 Unity（repression@2022.3.62f3）生成 `Act1_Room_ImportDef.asset` 与 `Act1_Room.asset`（DialogueGraphData），GraphId=`Act1_Room`。

## 验收方式
- 本地脚本校验对话图自洽：所有 NextId/NodeId 引用存在、阈值合法（✅ 26 节点 / 6 选项 / 10 个结束节点）。
- Unity 内反射调用 `DialogueCsvImportService.Import` 成功，读回资源确认 26 节点 + 3 组选项、文本/立绘Key/跳转正确，控制台零报错零警告。

## 风险与 TODO
- 立绘 `Portrait_LinGuNian` 尚未在 `DialoguePortraitLibrary` 映射（主角暂无立绘，系统会自动隐藏头像位，待美术补立绘后绑定即可）。
- 下一步：在 `Reality_Room` 场景搭 `DialogueRunner + DialoguePanel`，把各物体（冰箱/书桌/床头柜/手机/镜子）挂 `DialogueTrigger + DialogueFInteractController` 并绑定对应 StartNodeId，进 Play 联调应激度与过滤。
- 街道（场景三）、医院（场景四）对话已按同流程产出并导入：`Act1_Street`（19 节点/2 组选项，入口 13xxx）、`Act1_Hospital`（35 节点/1 组选项，入口 14xxx）。狗叫 PTSD +2、便利店脸色问询、母亲转述父亲来电等关键节点已落表。
- 仍待产出：场景五/六（回家发现入侵痕迹+噩梦碎片，`Act1_RoomNight`）、场景七~十（咨询室意识世界，含自由绘画疗法小游戏与吴良线索分支，`Act1_Counsel`）。后者含条件解锁（应激度≤30 线索对话）与剧情分支，需配合线索系统设计。
- 新增 NPC 立绘 Key：`Portrait_StoreOwner`（便利店老板）、`Portrait_LinMu`（林母）；护士为路人，无立绘。待美术补图后在 `DialoguePortraitLibrary` 映射。

## 对接说明
- 程序/策划：对话内容已可直接被 `DialogueRunner.StartDialogue("11200")` 等调用；调整剧本/数值改 CSV 后，在 `Act1_Room_ImportDef` Inspector 点"生成/更新"即可重导。
