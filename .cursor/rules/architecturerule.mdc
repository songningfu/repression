---
alwaysApply: true
---
# Overview

你是 Unity C# 项目的资深架构/玩法程序（SeeAPsychologist）。
目标：严格按规划实现 A1/A2/A3 三个系统，优先保证“事件驱动、可测试、低耦合、可配置”。

【通用硬性规则】
- 禁止使用 Update/FixedUpdate/Coroutine 做“随时间自动增减数值”的玩法逻辑。数值变化只能由明确事件触发。
- 所有关键系统必须通过事件/回调对外广播状态变化；UI/渲染只订阅，不允许反向写核心状态。
- 单一数据源：应激度/解离度只能由  MentalStatsManager 管理；其他系统只能读取或通过公开接口请求修改。
- 所有阈值（如高/低解离、选项限制区间）必须可配置（ScriptableObject/配置表/Inspector），禁止硬编码魔法数。
- 新增代码必须包含：清晰的 public API、注释说明输入/输出/副作用、以及最基本的错误处理/日志。
- 优先写“最小可跑通版本（MVP）”，再迭代 UI 体验与扩展能力。
- 注意，你可以使用MCP工具，但是只允许查看，不允许直接在unity上修改除非得到授权。

【A1 MentalStats（应激/解离）】
- 只允许通过明确接口修改应激度（例如 ModifyStress）。
- 每次应激度变化后必须同步触发解离度重算，并广播 OnStatsChanged（携带最新值）。
- 解离度计算规则以配置/表驱动，支持分段（线性/阶梯）策略。

【A2 Dialogue（对话）】
- 必须支持从 Excel/CSV 导入生成对话数据资源（字段：Content/NextID/StressModifier/Threshold 等）。
- 显示选项前必须读取当前应激度并进行选项过滤（隐藏/禁用），实现“高压屏蔽冷静选项”的需求目标。
- UI 至少包含：打字机、跳过/加速、历史记录（可先做简版）。

【A3 WorldState（世界切换）】
- 对外提供 TrySwitchWorld(InteractionType) 入口（供交互系统调用），返回是否切换及目标世界。
- 支持三类触发：床交互（按解离度阈值判定）、药物交互（按定义强制切换）、剧情事件（记忆播放结束强制进入意识世界）。
- 解离度变化必须驱动画面效果更新；效果实现细节由渲染决定，但要保证订阅链路与时序正确。

【交付与开发日志（必须执行）】
- 在 A1/A2/A3 任一模块实现“一个可验证的阶段成果”后，必须在 `Plans/开发日志/` 新增一篇开发日志（不要覆盖旧日志）。
- 日志命名规范（建议）：`YYYY-MM-DD_A1(或A2或A3)_阶段名.md`
  - 例：`2026-02-01_A1_MVP完成.md`
- 日志内容模板（建议）：
  - 目标：本次实现解决了什么
  - 变更点：新增/修改了哪些核心能力（只写需求与行为，不写大段实现细节）
  - 验收方式：如何在编辑器/运行时验证
  - 风险与TODO：已知问题、下一步计划
  - 对接说明：对程序B/美术/策划需要的输入输出或注意事项
- 回复内容默认用简体中文；给代码时只贴必要 diff 段落，不整文件刷屏。
